/** AppShell.Navbar width — keep in sync with App.tsx navbar config */
export const SIDEBAR_WIDTH_PX = 120;

/** AppShell.Footer height — keep in sync with App.tsx footer config */
export const DETAIL_FOOTER_HEIGHT_PX = 72;
